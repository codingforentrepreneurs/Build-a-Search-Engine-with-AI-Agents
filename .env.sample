# Database Configuration
# Option 1: Use a single connection URL (preferred)
DATABASE_URL=postgresql://user:password@localhost:5432/tars

# Option 2: Use individual PostgreSQL environment variables
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=tars
# PGUSER=user
# PGPASSWORD=password

# Bot name (used after running 'tars setup' to rename the bot)
# BOT_NAME=mybot
